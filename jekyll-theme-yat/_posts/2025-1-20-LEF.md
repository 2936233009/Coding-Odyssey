LEF文件是什么：
    1. 标准单元描述：LEF文件包含了标准单元（例如，逻辑门、触发器等）的几何和电气特性；
    2. 层信息：LEAF描述了用于设计的各个制作层（例如，金属层、多晶硅层等）的属性；
    3. 引脚信息：LEF包含有关标准单元输入/输出引脚的信息，包括他们与哪些层项链；
    4. 电气属性与时序：虽然不像其他专用格式（例如Liberty）那样详细，但LEF文件可以包含一些基础的电气属性和时序信息；
    5. 可重用性：由于LEF文件描述了单元的抽象和通用特性，这些单元可以在多个设计中重用；